<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- Màn hình chính điều phối các Fragment -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity thực thụ -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".LicenseActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".MovieActivity" />
        <activity android:name=".AddMovieActivity" />
        <activity android:name=".SplashScreenActivity" />

        <!-- LƯU Ý: HomeActivity, DiscoveryActivity, OffersActivity, ProfileActivity 
             đã được chuyển thành Fragment nên không khai báo là Activity ở đây -->

    </application>

</manifest>
